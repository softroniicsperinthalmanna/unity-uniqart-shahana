import 'package:flutter/material.dart';
class needy2 extends StatelessWidget {
  const needy2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
